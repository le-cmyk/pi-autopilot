[Manager]
# Aggressive shutdown: services get 10s, then SIGKILL
DefaultTimeoutStopSec=10s
# Reboot watchdog: force reboot if shutdown hangs more than 3 minutes
RebootWatchdogSec=3min
# Don't wait forever for devices
DefaultDeviceTimeoutSec=15s
